# declaring variables

auto x = 4;

# declare mutable variables

auto mut x = 4;

# force a type on a declaration

int x = 4;

# reassign mutable variables

x = 4;

# declare pointers

int *y = &x;

# declare functions

fn add(int x, int y) => int
{
   return x + y
}

# pass and return functions in functions

fn mult_function(fn some_func(int) => int, int x) => fn(int) => int

